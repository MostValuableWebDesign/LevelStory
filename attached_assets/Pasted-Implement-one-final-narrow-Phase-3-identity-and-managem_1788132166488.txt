Implement one final narrow Phase 3 identity and management-status correction.

Do not redesign the pilot, rebuild the historical index, or run the full 20+10 pilot yet.

1. Correct P identity at the ledger source

In buildHistoricalOccurrenceLedger, pOpenTimestamp must always represent the patience candle P open time.

Set:

pOpenTimestamp =
  new Date(patience.patienceCandle.openTime).toISOString()

Do not derive pOpenTimestamp from:

- linkedPullback.candle
- L timestamp
- eligibility event
- qualifying level interaction
- audit cursor

Keep:

- lTimestamp = qualifying interaction candle L
- pOpenTimestamp = patience candle P
- eOpenTimestamp = immediate next candle E open
- entryObservationTimestamp = completed E close

2. Add a source-level invariant

For every confirmed canonical patience occurrence:

- pOpenTimestamp equals patienceCandle.openTime
- eOpenTimestamp equals P close / immediate next five-minute open
- entryObservationTimestamp equals E close
- P and E belong to the same contract and New York trading date
- E immediately follows P with no later-candle confirmation

Reject or diagnose occurrences that violate this invariant. Do not silently repair them using L.

3. Add deterministic identity tests

Prove:

- One P→E pair linked to multiple qualifying levels produces one candidate.
- The qualifying levels merge as confluences.
- Two different P candles associated with the same L remain two physical occurrences.
- Changing L does not change the physical P→E identity.
- pOpenTimestamp never equals L unless L and P genuinely happen to be the same candle.
- Candidate identity remains source + formula + contract + date + direction + P + E.

4. Distinguish missing from invalid management

Change managementEvidenceStatus to support:

- complete
- missing
- invalid

Use missing only when required causal evidence is absent.

Use invalid when evidence exists but fails geometry, timestamp, quantity, or runner-direction validation.

For invalid management:

- Preserve the confirmed signal and candidate.
- Do not run outcome simulation.
- Keep the entry open and unscored.
- Report INVALID_MANAGEMENT_GEOMETRY with the exact reasons.
- Do not fabricate an exit or P&L.

5. Tighten runner activation geometry

Unless an explicit governed configuration permits equality:

- Long runner activation must be greater than entry.
- Short runner activation must be less than entry.

6. Verification

Run only focused:

- Phase 9 ledger tests
- Candidate identity tests
- Candidate execution tests
- Phase 3 pilot tests
- API typecheck
- git diff --check

Do not rebuild the historical index.
Do not run the full 20+10 pilot.
Do not alter strategy definitions, pullback tolerance, patience rules, entry cutoff, broker behavior, or live/paper trading.

Report the exact before/after physical identity and confirm that pOpenTimestamp is sourced exclusively from the patience candle.