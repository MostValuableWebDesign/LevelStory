Implement one targeted correctness pass for the LevelStory historical occurrence ledger, Visual Review, and strategy qualification engine.

Do not run a broad historical backtest, qualification batch, historical-index rebuild, or unrelated UI redesign. Preserve Shadow Mode only. Do not add live orders, paper orders, broker integration, fabricated candles, or fabricated fills.

OBJECTIVE

Correct the remaining causal-identity, occurrence-ledger, and qualification problems before any large backtest is run.

1. PRESERVE ALL VISUAL-REVIEW CATEGORIES

The current Visual Review path chooses ledger-derived categories instead of merging them with audit-derived categories whenever ledger occurrences exist.

Correct this behavior:

- Merge ledger-derived and audit-derived candidates.
- Do not drop strong breakout, consolidation, weak ORB probe, ambiguous candle, stop exit, target exit, or runner exit when occurrence-ledger records exist.
- Prefer occurrence-ledger evidence when an exact occurrence exists.
- Retain audit-derived evidence for categories the ledger does not represent.
- Deterministically deduplicate only genuinely identical candidates.
- Do not deduplicate separate occurrences merely because they share an audit ID, category, candle, or trading date.

2. MAKE EVERY OCCURRENCE AND SNAPSHOT IDENTITY UNIQUE

Every historical occurrence must have a stable `occurrenceId`.

Every Visual Review snapshot generated from an occurrence must include the occurrence identity in its deterministic snapshot ID.

The identity should include, as applicable:

- Source content fingerprint
- Formula/configuration hash
- Contract
- New York trading date
- Strategy/category
- Occurrence kind
- P timestamp
- Immediate E timestamp, or explicit absence
- Exact L event identity
- Evaluation cursor
- Audit ID
- Occurrence ID

Add a regression test with:

- One audit record
- Two occurrences in the same category
- Same trading date
- Distinct occurrence IDs
- Distinct snapshot IDs
- Separate persisted human reviews that cannot overwrite one another

3. PRESERVE THE EXACT CAUSAL CURSOR

Do not replace an occurrence’s evaluation cursor with the surrounding audit record’s later cursor.

For occurrence-based Visual Review snapshots:

- Persist the occurrence’s exact evaluation cursor.
- Use that cursor to determine machine-visible candles and evidence.
- Exclude every candle and event after that cursor.
- Related events must also be filtered against the occurrence cursor.
- Never allow a later audit prefix to expand an earlier occurrence’s machine-visible context.

Add a deterministic test where an occurrence is detected early in the session but also appears in a later audit prefix. The snapshot must stop at the original occurrence cursor.

4. LINK PATIENCE TO THE EXACT QUALIFYING L EVENT

Do not infer L by selecting the last pullback before P.

Create or preserve a stable causal link from the patience occurrence to the exact qualifying level-interaction event that made it eligible.

The occurrence must retain:

- Exact L timestamp
- Stable L event ID
- L candle OHLCV identity
- Every qualifying level at L
- Level type
- Level value at L
- Distance in MES ticks
- Configured tolerance
- Whether the interaction was wick touch, body touch, close proximity, or crossing

If VWAP or EMA 200 is involved, use its causal value at L, not its final session value.

When multiple levels qualify at the same L candle, retain all of them. Do not silently choose one.

5. ENFORCE IMMEDIATE P → E SEMANTICS

The entry-confirmation candle E must be the completed five-minute interval immediately after P.

Required behavior:

- P may only be confirmed by its immediate next interval E.
- If E is missing, incomplete, wrong-sided, or fails the confirmation buffer, P expires.
- A later completed candle must never be stored or displayed as E for that P.
- After failed E, scanning resumes for a new P2.
- Only the immediate candle after P2 may confirm P2.
- A later candle cannot confirm P1.

If useful for diagnostics, store a later observed candle separately as `nextObservedCandle`, but never label it as E, entry, trigger, or confirmation.

Add tests for:

- Successful P1 → E1
- Failed P1 → later P2 → successful E2
- Missing immediate E interval
- Immediate E incomplete
- Immediate E wrong-sided
- Later candle that would have passed but must not confirm P1
- Two successful P → E occurrences in one session

6. USE A CONTENT-BASED SOURCE FINGERPRINT

The historical source fingerprint must change when the underlying candle content changes, even if these remain identical:

- Filename
- Row count
- First timestamp
- Last timestamp
- Contract
- Selected dates

Use the authoritative importer/file fingerprint if available. Otherwise hash canonical contract-local candle bytes including timestamp, open, high, low, close, volume, contract, and completeness state.

Do not load the entire historical file into memory solely to hash it; use streaming or an existing importer fingerprint.

Add a test where one OHLC value changes but all metadata remains the same. The fingerprint and resulting occurrence identity must change.

7. SEPARATE ORB PULLBACK AND LEVEL-INTERACTION GATES

`ORB_BREAK_PULLBACK_CONTINUATION` must independently require:

- Finalized ORB/NTZ
- Completed breakout outside the ORB/NTZ
- A genuine qualifying pullback event
- Interaction with at least one structural, dynamic, or Fibonacci level
- Patience candle P
- Immediate next-candle E confirmation
- Confirmation buffer
- Risk approval

Do not allow Fibonacci proximity alone to substitute for the existence of a pullback.

A Fibonacci interaction qualifies only when tied to the actual pullback/L event. Do not scan ordinary continuation candles and call them pullbacks merely because they approach a Fibonacci level.

Keep the governed pullback proximity tolerance at 12 MES ticks unless an existing strategy-specific configuration explicitly overrides it.

Add tests proving:

- Pullback without qualifying level interaction fails.
- Fibonacci touch without a genuine pullback fails.
- Genuine pullback plus structural level passes the interaction gate.
- Genuine pullback plus causal VWAP interaction passes.
- Genuine pullback plus causal EMA 200 interaction passes.
- Genuine pullback plus valid Fibonacci interaction passes.
- No strong-breakout classification is required for ORB continuation.

8. REQUIRE GENUINE EQUIVALENT-REVERSAL DIRECTION EVIDENCE

Do not mark directional confirmation as passed merely because the engine reversed the preceding trend direction programmatically.

`EQUIVALENT_CANDLE_REVERSAL` must require governed reversal-direction evidence, such as the existing equivalent-candle reversal structure plus an actual reversal confirmation condition.

The preceding 15-minute trend may be opposite the reversal, but the reversal direction must be established from causal completed-candle evidence—not assigned synthetically.

Preserve:

- Equivalent full-body candle evidence
- Reversal confirmation
- Patience P
- Immediate E confirmation
- Risk approval

If P → E is intended to be the only reversal-direction confirmation, rename the audit rule and explanation honestly instead of claiming independent directional evidence.

Add tests for:

- Equivalent candles without reversal confirmation fail.
- Equivalent candles with confirmed reversal and valid P → E pass.
- Reversal may oppose the preceding trend.
- Synthetic reverse-direction assignment alone cannot qualify.

9. AUDIT AND VISUAL-REVIEW REQUIREMENTS

For every displayed occurrence, expose concise evidence for:

- Strategy
- Occurrence ID
- L candle and qualifying levels
- P candle
- Immediate E candle or explicit failure reason
- Exact evaluation cursor
- Machine-visible candle count
- Source fingerprint
- Formula/configuration hash
- Qualification or rejection reason

Keep machine evidence immutable and separate from persisted human teaching submissions.

Do not allow a human submission to mutate the active formula automatically. Existing proposal, approval, audit, and Shadow Mode governance must remain intact.

10. FOCUSED VERIFICATION ONLY

Add focused deterministic tests for every corrected behavior.

Run only:

- Relevant Phase 5 tests
- Relevant Phase 6 tests
- Phase 9 occurrence-ledger tests
- Visual-validation tests
- Teaching-storage/review identity tests if affected
- API typecheck
- Frontend typecheck
- git diff --check

Do not run:

- Cold 20+2 replay
- Full historical qualification batch
- Broad historical-index rebuild
- Expensive unrelated test suites

FINAL REPORT

Report:

1. Files changed.
2. Root cause of each issue.
3. Final occurrence identity fields.
4. How exact L → P → E linkage is preserved.
5. How missing or failed immediate E is represented.
6. How Visual Review merges ledger and audit categories.
7. ORB’s separate pullback and level-interaction gates.
8. Equivalent-reversal confirmation rule.
9. Test totals and typecheck results.
10. Any remaining limitation.

Do not claim completion if occurrence-specific causal cursors, unique snapshot identities, exact L linkage, or immediate P → E behavior are not covered by deterministic tests.