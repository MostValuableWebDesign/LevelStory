Implement one narrow Phase 2E correction to make trade candidates authoritative, reconcile modeled trades, and fix the upstream arm-state conflicts.

Do not change the frontend, charts, strategy definitions, patience-candle shape, P→immediate-E confirmation, 12-tick level tolerance, primary entry window, historical index, broker behavior, or live/paper trading safeguards.

CURRENT VERIFIED PROBLEMS

1. Candidate records are currently constructed temporarily inside `historicalReplayDiagnostics()`:

```ts
const candidateRecords = confirmedPatience.flatMap(...)
These are diagnostic projections—not authoritative candidate records used by the replay and modeled-trade pipeline.
The cached replay reports:
6 candidates
6 modeled trades
2 candidates without modeled trades
2 modeled trades without candidates
Therefore only four candidates and modeled trades are actually linked consistently.
Modeled trades without eligible candidates may still be included in backtest trades, P&L, win rate, and drawdown even though the corrected strategy would not take them.

The replay reports 55 conflicting terminal arm states. Deduplicating and retaining the first observed state hides inflation but does not correct the Phase 5 producer that emits conflicting outcomes.

Make only the following corrections.
ADD AN AUTHORITATIVE TRADE-CANDIDATE MODEL
Create an authoritative candidate type, for example:
type HistoricalTradeCandidate = {
  candidateId: string;
  signalOccurrenceId: string;
  sourceFingerprint: string;
  formulaHash: string;
  formulaVersion: string;
  contractSymbol: string;
  tradingDate: string;
  direction: "long" | "short";
  primaryEdge: string;
  matchedEdges: string[];
  supportingConfluences: string[];
  qualifyingLevelIdentifiers: string[];
  qualifyingLevelValues: Record<string, number>;
  patienceTimestamp: string;
  expectedEntryTimestamp: string;
  confirmationPrice: number;
  confirmationBufferTicks: number;
  grade: string;
  eligible: true;
};
Use existing project naming where appropriate.
Candidate identity must be deterministic:
source fingerprint
+ formula/configuration hash
+ canonical signal occurrence ID
One eligible canonical signal must produce exactly one candidate.
Multiple edges and confluences must remain arrays on the same candidate.
Do not create candidates inside diagnostics. Candidate construction must be an authoritative replay step.
PROJECT CANDIDATES FROM CANONICAL SIGNALS
After building the canonical occurrence ledger:
Select canonical SIGNAL_CONFIRMED occurrences.
Apply complete edge eligibility.
Apply the governed 9:30 a.m.–1:00 p.m. ET entry window.
Project exactly one authoritative candidate for each eligible signal.
Preserve rejected confirmed evidence separately with exact rejection reasons.
For ORB_PULLBACK_CONTINUATION, require:
Finalized ORB/NTZ
Completed breakout
Genuine pullback
Persisted qualifying level interaction
Valid P
Immediate E
Confirmation buffer
Entry inside the primary window
Do not project level-less or out-of-window ORB signals.
Generic patience, consolidation breakout, and equivalent reversal must continue to use their own complete edge requirements.
STORE CANDIDATES IN THE BACKTEST REPORT
Add authoritative candidates to the backtest report:
tradeCandidates: HistoricalTradeCandidate[];
If useful, also add:
rejectedCandidateSignals: {
  signalOccurrenceId: string;
  reasonCodes: string[];
  details: string[];
}[];
Visual Review and later qualification workflows should be able to read these records, but do not redesign the frontend in this task.
Do not mutate machine evidence through human reviews.
REQUIRE MODELED TRADES TO REFERENCE CANDIDATES
Every authoritative modeled trade must contain:
signalOccurrenceId
candidateId
Reconcile raw modeled trades using the exact physical signal identity:
Contract
Trading date
Direction
P timestamp
Immediate E timestamp
Do not match using only date, direction, or entry price.
A modeled trade without an eligible candidate is an orphan and must not remain in the authoritative trade collection.
Preserve orphan evidence separately:
orphanModeledTrades: {
  tradeId: string;
  reason: string;
  matchingSignalOccurrenceId?: string;
}[];
Do not delete audit evidence.
EXCLUDE ORPHAN MODELED TRADES FROM METRICS
Calculate authoritative:
Net P&L
Gross P&L
Win rate
Loss rate
Fees
Slippage
Maximum drawdown
Stop/target outcomes
Trade count
using only modeled trades that reference an eligible authoritative candidate.
Do not include orphan modeled trades in:
report.trades
metrics
segments
qualification results
edge statistics
They may remain in a diagnostic collection with their rejection reason.
Recalculate all trade-based metrics after candidate reconciliation.
PRESERVE CANDIDATES WITHOUT MODELED TRADES
A valid candidate may lack a modeled trade because:
Required fill evidence is unavailable
Quote data is unavailable
Same-candle execution order is ambiguous
Immediate confirmation price cannot be modeled honestly
Another explicit execution limitation exists
Keep the candidate.
Record the exact reason:
executionStatus:
  | "MODELED_TRADE_CREATED"
  | "EXECUTION_EVIDENCE_UNAVAILABLE"
  | "EXECUTION_AMBIGUOUS";
Do not fabricate bid/ask quotes, fills, prices, P&L, or outcomes.
Under OHLCV-only data, a candidate remains evidence that the strategy produced an entry signal even if an honest fill cannot be modeled.
MAKE DIAGNOSTICS READ AUTHORITATIVE RECORDS
Remove temporary candidate construction from historicalReplayDiagnostics().
Pass the actual collections into diagnostics:
historicalReplayDiagnostics(
  audits,
  occurrences,
  tradeCandidates,
  authoritativeModeledTrades,
  rejectedCandidateSignals,
  orphanModeledTrades
)
Compute using actual ID joins:
Canonical confirmed signals
Eligible candidates
Rejected confirmed signals
Confirmed signals without candidates
Candidates without confirmed signals
Candidates with modeled trades
Candidates without modeled trades
Modeled trades without candidates
Duplicate candidates per signal
Duplicate modeled trades per candidate
Orphan modeled trades excluded from metrics
Do not compute these through simple subtraction when stable-ID joins are available.
FIX THE UPSTREAM ARM STATE MACHINE
Correct Phase 5 so one arm cannot emit conflicting terminal outcomes across later replay cursors.
Authoritative arm states:
ACTIVE
CONSUMED
INVALIDATED
SUPERSEDED
Permitted transitions:
ACTIVE → CONSUMED
ACTIVE → INVALIDATED
ACTIVE → SUPERSEDED
Terminal states are immutable.
Rules:
A consumed arm cannot later become superseded.
An invalidated arm cannot later become consumed.
A superseded arm cannot later confirm a signal.
A later eligibility event supersedes only an arm that is still ACTIVE.
Replaying a longer candle prefix must reproduce the same earlier terminal transition.
Transition timestamp is the causal candle/event time—not the audit cursor.
Later audit observations reference the stored terminal state without creating or changing it.
Review the code that maps earlier occurrences to superseded. It must not overwrite occurrences already marked consumed or invalidated.
HANDLE SAME-TIMESTAMP TERMINAL CONFLICTS
Sort transitions by actual causal transition timestamp before resolution.
If two different terminal states occur at the exact same timestamp:
Apply an existing governed same-candle policy if one exists.
Otherwise record:
ARM_TERMINAL_AMBIGUITY
Do not silently select based on array order.
Do not project a candidate from an unresolved ambiguous arm.
Do not fabricate an ordering.
The final cached replay should ideally produce zero terminal conflicts. Any remaining conflict must list:
Arm ID
Competing terminal states
Exact timestamps
Audit IDs
Candles/events responsible
RECONCILE THE JULY 13–27 REPLAY
Explicitly report the two current modeled trades without candidates:
Trade IDs
Sessions
Directions
Entry timestamps
Matching signal IDs, if any
Why their candidates were rejected
Confirmation that they are excluded from authoritative P&L
Explicitly report the two candidates without modeled trades:
Candidate IDs
Signal occurrence IDs
Sessions
P and E timestamps
Why an honest modeled trade was unavailable
Confirmation that the candidate remains present
DETERMINISTIC TESTS
Add focused tests proving:
A. Authoritative candidate materialization
One eligible canonical signal produces one stored candidate.
Diagnostics read that candidate record.
B. Candidate identity
Repeated audit references and multiple confluences produce one candidate ID.
C. Modeled-trade linkage
A modeled trade references both signal occurrence ID and candidate ID.
D. Orphan exclusion
A modeled trade with no eligible candidate is moved to orphan diagnostics.
It is excluded from report trades and P&L.
E. Candidate without execution evidence
Candidate remains stored.
No modeled trade or fill is fabricated.
Execution status explains why.
F. Metric recalculation
One linked winning trade and one orphan losing trade are supplied.
Metrics include only the linked winning trade.
G. Consumed-arm stability
An arm confirms P→E and becomes consumed.
Later eligibility events cannot mark it superseded.
H. Invalidated-arm stability
An invalidated arm remains invalidated in every later audit prefix.
I. Supersession
A newer pullback supersedes only a still-active prior arm.
J. Same-timestamp conflict
Conflicting terminal states at the same causal time are resolved by governed policy or marked ambiguous.
Array iteration order cannot change the result.
K. Candidate window and level rules
Level-less ORB signal produces no candidate.
Out-of-window signal produces no candidate.
Valid in-window ORB signal produces one candidate.
L. Stable reconciliation
Every authoritative modeled trade has exactly one candidate.
Every candidate has exactly one confirmed canonical signal.
VERIFICATION
Run only:
Focused Phase 5 arm-state tests
Focused Phase 6 edge-eligibility tests
Focused Phase 9 ledger, candidate, trade-linkage, and metric tests
API typecheck
git diff --check
One cached July 13–27 replay
Do not:
Rebuild the historical index
Run a broad backtest
Change the frontend
Change the formula
Change the 12-tick tolerance
Change the 9:30–1:00 ET entry window
Add live or paper trading
Fabricate quotes, fills, or outcomes
FINAL REPORT
Provide:
Files changed.
Authoritative candidate type and identity.
Where candidates are created and stored.
How modeled trades reference candidates.
The two former modeled trades without candidates and their final disposition.
The two candidates without modeled trades and exact execution limitations.
Before/after authoritative trade count.
Before/after P&L and drawdown if orphan trades previously affected metrics.
Unique arm count and each terminal-state count.
Remaining arm conflicts with full evidence.
Confirmed signals, rejected signals, candidates, linked modeled trades, and orphan modeled trades.
Focused test totals.
Remaining limitations without unrelated implementation.
SUCCESS CRITERIA
These invariants must hold:
eligible canonical signal
→ exactly one authoritative candidate
authoritative modeled trade
→ exactly one candidate
→ exactly one canonical signal
modeledTradesWithoutCandidates = 0
duplicateCandidatesPerSignal = 0
duplicateModeledTradesPerCandidate = 0
armInvalidations
+ armSupersessions
+ armConsumptions
<= uniqueArms
Backtest metrics must use only modeled trades linked to eligible candidates.
```